import { buildBacklinksDryRunPlan } from "./backlinks-plan";
import type { PrepareBacklinksAutomationRunDependencies, PrepareBacklinksAutomationRunInput, PrepareBacklinksAutomationRunResult } from "./preparation-types";
const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
function validate(i:PrepareBacklinksAutomationRunInput){if(!UUID.test(i.workspaceId))throw new Error("workspaceId must be a valid UUID");if(i.requestedBy!==null&&!UUID.test(i.requestedBy))throw new Error("requestedBy must be a valid UUID");if(!i.idempotencyKey.trim()||i.idempotencyKey!==i.idempotencyKey.trim()||i.idempotencyKey.length>255)throw new Error("idempotencyKey must be trimmed and at most 255 characters");if(!["manual","scheduled","internal"].includes(i.triggerSource))throw new Error("triggerSource must be valid");if(!Number.isFinite(Date.parse(i.scheduledAt)))throw new Error("scheduledAt must be a valid date");for(const [v,n] of [[i.discoveryInput,"discoveryInput"],[i.qualificationInput,"qualificationInput"]] as const)if(typeof v!=="object"||v==null||Array.isArray(v))throw new Error(`${n} must be a JSON object`)}
export async function prepareBacklinksAutomationRun(d:PrepareBacklinksAutomationRunDependencies,i:PrepareBacklinksAutomationRunInput):Promise<PrepareBacklinksAutomationRunResult>{validate(i);const created=await d.createRun({workspaceId:i.workspaceId,system:"backlinks",runKind:"backlinks.daily_preview",idempotencyKey:i.idempotencyKey,mode:"dry_run",triggerSource:i.triggerSource,requestedBy:i.requestedBy,scheduledAt:i.scheduledAt,input:{discovery:i.discoveryInput,qualification:i.qualificationInput}});if(created.kind==="rejected")return created;const plan=buildBacklinksDryRunPlan({workspaceId:i.workspaceId,runId:created.run.id,scheduledAt:i.scheduledAt,discoveryInput:i.discoveryInput,qualificationInput:i.qualificationInput});const first=await d.createTask({...plan.tasks[0],maxAttempts:3,backoffBaseSeconds:60});const second=await d.createTask({...plan.tasks[1],maxAttempts:3,backoffBaseSeconds:60});return{kind:"prepared",run:created.run,runDisposition:created.kind,tasks:[{disposition:first.kind,task:first.task},{disposition:second.kind,task:second.task}]}}
