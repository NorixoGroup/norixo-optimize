import { readFile } from "node:fs/promises";
async function main() { const source = await readFile("app/(default)/dashboard/backlinks/page.tsx", "utf8"); const start = source.indexOf("const handleConfirmInboundReplyClassification"); const end = source.indexOf("const openOutreachUnknownResolutionDialog", start); if (start < 0 || end <= start) throw new Error("Classification handler missing"); const handler = source.slice(start, end); for (const value of ["pending.classification !== \"positive\"", "pending.classification !== \"negative\"", "method: \"POST\"", "JSON.stringify({ confirm: true, classification: pending.classification })", "reloadInboundReplies", "loadDashboard()", "setInboundReplyPendingConfirmation(null)", "setInboundReplySubmittingMessageId(null)"]) if (!handler.includes(value)) throw new Error(`Missing ${value}`); for (const forbidden of ["idempotencyKey", "stopReason", "workspaceId", "actorUserId", "/send", "outreachEmailProvider", "scheduler", "follow_up"]) if (handler.includes(forbidden)) throw new Error(`Forbidden ${forbidden}`); console.log("PASS — Backlink inbound reply classification UI smoke"); }
void main();
