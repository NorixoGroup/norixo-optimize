import { readFile } from "node:fs/promises";
function assert(v: unknown, m: string): asserts v { if (!v) throw new Error(m); }
async function main() { const [page, dialog] = await Promise.all([readFile("app/(default)/dashboard/backlinks/page.tsx", "utf8"), readFile("app/(default)/dashboard/backlinks/_components/OutreachDraftEditDialog.tsx", "utf8")]); for (const value of ["Modifier le brouillon", 'outreach.status === "draft"', "openOutreachDraftEditDialog", "setOutreachDraftEditContactId(contactId)", "setOutreachDraftEditChannel(channel)", "setOutreachDraftEditSubject", "setOutreachDraftEditBody", "setOutreachDraftEditChannel(\"\")", "/api/backlinks/outreach/${outreachDraftEditDialog.id}/draft", "await loadDashboard()", "outreachDraftEditSubmitting"]) assert(page.includes(value), `Missing ${value}`); assert(page.includes('JSON.stringify({ subject: outreachDraftEditSubject || null, body: outreachDraftEditBody || null, contactId: outreachDraftEditContactId, channel: outreachDraftEditChannel })'), "PATCH payload must be exact."); for (const forbidden of ["Send", "Ready", "template", "apiRequest"]) assert(!dialog.includes(forbidden), `Forbidden UI ${forbidden}`); console.log("PASS — Backlink outreach draft edit UI smoke"); }
void main();
