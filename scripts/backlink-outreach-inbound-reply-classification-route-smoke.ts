import { readFile } from "node:fs/promises";
function assert(value: unknown, message: string): asserts value { if (!value) throw new Error(message); }
async function main() { const source = await readFile("app/api/backlinks/outreach/[id]/inbound/[messageId]/classify/route.ts", "utf8"); for (const value of ["export async function POST", "getRequestUserAndWorkspace(request)", 'auth.status === "unauthenticated"', 'auth.status === "workspace_forbidden"', "isAdminPrivateEmail", "createSupabaseAdminClient", "const adminClient = createSupabaseAdminClient()", "Object.keys(body).length !== 2", "body.confirm !== true", 'body.classification !== "positive"', 'body.classification !== "negative"', "workspaceId: auth.workspace.id", "actorUserId: auth.user.id", "outreachId: id", "inboundMessageId: messageId", "getBacklinkOutreachById(adminClient", "getBacklinkOutreachInboundMessageById(adminClient", "classifyRpc(adminClient", "ok: true", "status: 401", "status: 403", "status: 404", "status: 409"]) assert(source.includes(value), `Missing route invariant: ${value}`); for (const forbidden of ["body.workspaceId", "body.actorUserId", "body.classifiedAt", "body.status", "body.stopReason", "body.responseType", "body.outreachId", "body.messageId", "/send", "outreachEmailProvider", "sendTransactionalEmail", "scheduler", "follow_up"]) assert(!source.includes(forbidden), `Forbidden route behavior: ${forbidden}`); console.log("PASS — Backlink inbound reply classification route smoke"); }
void main();
