import { readFile } from "node:fs/promises";
function assert(v: unknown, m: string): asserts v { if (!v) throw new Error(m); }
async function main() { const [page, dialog] = await Promise.all([readFile("app/(default)/dashboard/backlinks/page.tsx", "utf8"), readFile("app/(default)/dashboard/backlinks/_components/OutreachReadyDialog.tsx", "utf8")]); for (const value of ["OutreachReadyDialog", "Marquer comme prêt", 'outreach.status === "draft"', "outreachReadySubmitting", "handleMarkOutreachReady", "JSON.stringify({ confirm: true })", "await loadDashboard()", "setOutreachReadyDialog(null)"]) assert(page.includes(value), `Missing ${value}`); assert(!page.includes('JSON.stringify({ confirm: true,') && !page.includes('confirm: true, subject'), "Ready payload must only confirm."); for (const value of ["outreach.contact", "Aucun message ne sera envoyé", "Annuler", "Marquer comme prêt", "outreach.subject", "outreach.body"]) assert(dialog.includes(value), `Missing dialog content ${value}`); for (const forbidden of ["Envoyer", "provider", "follow-up", "scheduler", "apiRequest"]) assert(!dialog.includes(forbidden), `Forbidden ${forbidden}`); console.log("PASS — Backlink outreach ready UI smoke"); }
void main();
