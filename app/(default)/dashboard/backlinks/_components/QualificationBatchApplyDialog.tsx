import React from "react";

type Result = { updated: number; existing: number; notApplicable: number; failed: number };
type Props = { count: number; submitting: boolean; error: string | null; result: Result | null; handoffCount: number; onClose: () => void; onConfirm: () => void; onPrepareCampaign: () => void };

export default function QualificationBatchApplyDialog({ count, submitting, error, result, handoffCount, onClose, onConfirm, onPrepareCampaign }: Props) {
  return <div role="dialog" aria-modal="true" aria-labelledby="qualification-batch-apply-title" className="fixed inset-0 z-50 flex items-end bg-slate-950/40 p-4 sm:items-center sm:justify-center"><div className="w-full max-w-xl rounded-3xl bg-white p-6 shadow-2xl"><h2 id="qualification-batch-apply-title" className="text-xl font-semibold text-slate-950">Appliquer les qualifications sélectionnées ?</h2><p className="mt-1 text-sm text-slate-600">{count} opportunité{count > 1 ? "s" : ""} sera{count > 1 ? "ont" : ""} traitée{count > 1 ? "s" : ""}.</p>{error ? <p role="alert" className="mt-4 rounded-xl bg-rose-50 p-3 text-sm text-rose-800">{error}</p> : null}{result ? <div role="status" className="mt-4 rounded-xl bg-slate-50 p-3 text-sm text-slate-700"><p>Mises à jour : {result.updated}</p><p>Déjà appliquées : {result.existing}</p><p>Non applicables : {result.notApplicable}</p><p>Échecs : {result.failed}</p>{result.failed > 0 ? <p className="mt-2 font-semibold text-amber-800">Application partielle : vous pouvez resélectionner les éléments en échec.</p> : null}{handoffCount > 0 ? <button type="button" onClick={onPrepareCampaign} className="mt-3 rounded-full border border-slate-900 px-3 py-1 text-xs font-semibold text-slate-900">Préparer une campagne ({handoffCount})</button> : null}</div> : null}<div className="mt-6 flex justify-end gap-3"><button type="button" onClick={onClose} disabled={submitting} className="rounded-full px-4 py-2 text-sm font-semibold text-slate-700 disabled:opacity-50">Annuler</button><button type="button" onClick={onConfirm} disabled={submitting || result !== null} className="rounded-full bg-slate-900 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">{submitting ? "Application…" : "Appliquer"}</button></div></div></div>;
}
