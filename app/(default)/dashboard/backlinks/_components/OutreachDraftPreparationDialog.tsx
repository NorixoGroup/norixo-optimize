import React from "react";

type Contact = { contactId: string; label: string; eligibleChannels: readonly ("email" | "linkedin" | "contact_form")[] };
type Props = { opportunityLabel: string; contacts: readonly Contact[]; contactId: string; channel: "email" | "linkedin" | "contact_form" | ""; loading: boolean; previewLoading: boolean; submitting: boolean; preview: { subject: string | null; body: string } | null; success: string | null; error: string | null; onClose: () => void; onContactChange: (value: string) => void; onChannelChange: (value: "email" | "linkedin" | "contact_form" | "") => void; onPreview: () => void; onApply: () => void };
export default function OutreachDraftPreparationDialog({ opportunityLabel, contacts, contactId, channel, loading, previewLoading, submitting, preview, success, error, onClose, onContactChange, onChannelChange, onPreview, onApply }: Props) { const contact = contacts.find((item) => item.contactId === contactId); return <div role="dialog" aria-modal="true" aria-labelledby="outreach-draft-title" className="fixed inset-0 z-50 flex items-end bg-slate-950/40 p-4 sm:items-center sm:justify-center"><div className="w-full max-w-xl rounded-3xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between gap-4"><div><h2 id="outreach-draft-title" className="text-xl font-semibold text-slate-950">Préparer un outreach</h2><p className="mt-1 text-sm text-slate-600">{opportunityLabel}</p></div><button type="button" onClick={onClose} disabled={submitting} className="rounded-full px-3 py-1 text-sm font-semibold text-slate-600">Fermer</button></div>{loading ? <p className="mt-4 text-sm text-slate-600">Chargement des contacts éligibles…</p> : <><label className="mt-5 block text-sm font-semibold text-slate-700">Contact<select value={contactId} onChange={(event) => onContactChange(event.target.value)} className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2 text-sm"><option value="">Sélectionner un contact</option>{contacts.map((item) => <option key={item.contactId} value={item.contactId}>{item.label}</option>)}</select></label><label className="mt-4 block text-sm font-semibold text-slate-700">Canal<select value={channel} onChange={(event) => onChannelChange(event.target.value as Props["channel"])} disabled={!contact} className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2 text-sm"><option value="">Sélectionner un canal</option>{contact?.eligibleChannels.map((item) => <option key={item} value={item}>{item}</option>)}</select></label><button type="button" onClick={onPreview} disabled={!contactId || !channel || previewLoading || submitting} className="mt-4 rounded-full border border-slate-300 px-4 py-2 text-sm font-semibold disabled:opacity-50">{previewLoading ? "Prévisualisation…" : "Prévisualiser"}</button>{preview ? <div className="mt-4 rounded-xl border border-slate-200 bg-slate-50 p-3 text-sm"><p className="font-semibold">{preview.subject ?? "Sans objet"}</p><pre className="mt-2 whitespace-pre-wrap font-sans text-slate-700">{preview.body}</pre></div> : null}<button type="button" onClick={onApply} disabled={!preview || submitting || previewLoading} className="mt-5 rounded-full bg-slate-900 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">{submitting ? "Création…" : "Créer le brouillon"}</button></>}{error ? <p role="alert" className="mt-4 text-sm text-rose-800">{error}</p> : null}{success ? <p className="mt-4 text-sm text-emerald-800">{success}</p> : null}</div></div>; }
