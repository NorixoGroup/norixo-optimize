import React from "react";
type Props = { outreach: { contact: string; channel: string; subject: string | null; body: string | null }; submitting: boolean; error: string | null; success: string | null; onClose(): void; onConfirm(): void };
export default function OutreachReadyDialog({ outreach, submitting, error, success, onClose, onConfirm }: Props) { return <div role="dialog" aria-modal="true" aria-labelledby="outreach-ready-title" className="fixed inset-0 z-50 bg-slate-950/40 p-4"><div className="mx-auto mt-12 max-w-xl rounded-3xl bg-white p-6"><h2 id="outreach-ready-title">Marquer comme prêt</h2><p>{outreach.contact} · {outreach.channel}</p><p>{outreach.subject ?? "Sans objet"}</p><pre className="whitespace-pre-wrap">{outreach.body}</pre><p>Aucun message ne sera envoyé. Cette action marque uniquement ce brouillon comme prêt.</p>{error ? <p role="alert">{error}</p> : null}{success ? <p>{success}</p> : null}<button type="button" disabled={submitting} onClick={onClose}>Annuler</button><button type="button" disabled={submitting} onClick={onConfirm}>{submitting ? "Validation…" : "Marquer comme prêt"}</button></div></div>; }
