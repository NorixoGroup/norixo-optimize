import React from "react";
type Option = { id: string; label: string };
type Props = { searchQuery: string; statusFilter: string; domainFilter: string; assetFilter: string; outreachFilter: string; domains: readonly Option[]; assets: readonly Option[]; outreach: readonly Option[]; resultCount: number; totalCount: number; filtersActive: boolean; onSearchQueryChange: (value: string) => void; onStatusFilterChange: (value: string) => void; onDomainFilterChange: (value: string) => void; onAssetFilterChange: (value: string) => void; onOutreachFilterChange: (value: string) => void; onReset: () => void; statusLabel: (value: string) => string; };
export default function LinkFilters({ searchQuery, statusFilter, domainFilter, assetFilter, outreachFilter, domains, assets, outreach, resultCount, totalCount, filtersActive, onSearchQueryChange, onStatusFilterChange, onDomainFilterChange, onAssetFilterChange, onOutreachFilterChange, onReset, statusLabel }: Props) { return <div className="mb-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-5"><label className="text-sm font-semibold text-slate-700">Rechercher<input value={searchQuery} onChange={(event) => onSearchQueryChange(event.target.value)} placeholder="Rechercher une URL, un domaine, un asset ou un outreach…" className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2 text-sm font-normal" /></label><label className="text-sm font-semibold text-slate-700">Statut<select value={statusFilter} onChange={(event) => onStatusFilterChange(event.target.value)} className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2 text-sm font-normal"><option value="">Tous les statuts</option>{["active","pending_verification","lost","removed"].map((status) => <option key={status} value={status}>{statusLabel(status)}</option>)}</select></label><label className="text-sm font-semibold text-slate-700">Domaine<select value={domainFilter} onChange={(event) => onDomainFilterChange(event.target.value)} className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2 text-sm font-normal"><option value="">Tous les domaines</option>{domains.map((item) => <option key={item.id} value={item.id}>{item.label}</option>)}</select></label><label className="text-sm font-semibold text-slate-700">Asset<select value={assetFilter} onChange={(event) => onAssetFilterChange(event.target.value)} className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2 text-sm font-normal"><option value="">Tous les assets</option>{assets.map((item) => <option key={item.id} value={item.id}>{item.label}</option>)}</select></label><label className="text-sm font-semibold text-slate-700">Outreach<select value={outreachFilter} onChange={(event) => onOutreachFilterChange(event.target.value)} className="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2 text-sm font-normal"><option value="">Tous les Outreach</option>{outreach.map((item) => <option key={item.id} value={item.id}>{item.label}</option>)}</select></label><div className="sm:col-span-2 xl:col-span-5 flex items-center justify-between gap-3 text-sm text-slate-600"><span>{resultCount} résultat{resultCount === 1 ? "" : "s"}{filtersActive ? ` sur ${totalCount}` : ""}</span><button type="button" onClick={onReset} disabled={!filtersActive} className="font-semibold underline decoration-slate-300 underline-offset-4 disabled:opacity-50">Réinitialiser</button></div></div>; }
